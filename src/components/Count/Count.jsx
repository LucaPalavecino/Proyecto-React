import React, {useState} from 'react';

const Count = ({initial,stock}) => {

    console.log(initial)
    console.log(stock)

    return (
        <div>
            <p>Agregar al carrito</p>
        </div>
    );
};

export default Count;