import React, {useState} from 'react';

const Count = ({inicial,stock}) => {

    console.log(inicial)
    console.log(stock)

    return (
        <div>
            <p>Agregar al carrito</p>
        </div>
    );
};

export default Count;